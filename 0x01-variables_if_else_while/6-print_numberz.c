#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	int numbers;

	for (numbers = 48; numbers <= 57; numbers++)

	{
		printf("%c", numbers);
	}
	printf("\n");
	return (0);
}
