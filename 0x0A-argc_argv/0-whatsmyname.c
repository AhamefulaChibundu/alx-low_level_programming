#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Description: prints the name of the program
 * @argc: argument count
 * @argv: Argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
